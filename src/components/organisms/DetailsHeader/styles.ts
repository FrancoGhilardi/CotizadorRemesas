import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    height: 70,
    borderBottomWidth: 2,
    flexDirection: 'row',
    alignItems: 'center',
    paddingHorizontal: '5%',
  },
});
