import {StyleSheet} from 'react-native';
import {Colors} from '../../../constants/Colors';

export const styles = StyleSheet.create({
  subcontainer: {
    backgroundColor: Colors.PrimaryP08,
    justifyContent: 'space-evenly',
    height: '100%',
    width: '90%',
    borderRadius: 20,
    alignItems: 'center',
  },
});
